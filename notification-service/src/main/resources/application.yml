spring:
  application:
    name: notification-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://config-user:config-pass@localhost:8888
      fail-fast: false
